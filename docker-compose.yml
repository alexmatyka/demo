version: '3.8'

services:
  frontend:
    build:
      context: ./STARTER
    ports:
      - "5173:5173" 
    volumes:
      - ./STARTER:/app             
      - /app/node_modules          
    working_dir: /app
    command: npm run dev -- --host
